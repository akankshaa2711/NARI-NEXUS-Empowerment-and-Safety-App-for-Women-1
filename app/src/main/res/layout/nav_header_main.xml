<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/your_header_gradient_or_color_if_any">

    <!-- Profile circle -->
    <ImageView
        android:id="@+id/drawer_profile_pic"
        android:layout_width="84dp"
        android:layout_height="84dp"
        android:src="@drawable/outline_account_circle_24"
        android:scaleType="centerCrop"
        android:layout_gravity="center"/>

    <TextView
        android:id="@+id/drawer_profile_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your Name"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingTop="8dp"
        android:textColor="#000000"/>

    <TextView
        android:id="@+id/drawer_profile_sub"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Short bio or ID"
        android:gravity="center"
        android:textColor="#444444"
        android:paddingBottom="8dp"/>
    <AutoCompleteTextView
        android:id="@+id/nav_search"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:hint="Search features..."
        android:padding="8dp"
        android:background="@drawable/search_edit_background"
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:drawableLeft="@drawable/ic_search" />


</LinearLayout>
